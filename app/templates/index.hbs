{{layer-picker
  onLayerUpdate=(action "onLayerUpdate")
  layersChosen=(readonly model.layersChosen)
  layersAvailable=(readonly model.layersAvailable)}}

{{geoserver-leaflet
  layers=model.layersChosen}}

{{outlet}}
